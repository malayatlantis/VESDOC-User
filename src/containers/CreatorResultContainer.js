import ChannelSearchResult from '../components/SearchComponents/channelSearchResult'
import {connect} from 'react-redux'

const mapStateToProps=state=>({

})

const mapDispatchToProps=dispatch=>({

})

export default connect(mapDispatchToProps,mapStateToProps)(ChannelSearchResult)