import PurchasePlaylist from '../components/Purchase_componrnts/purchasePlaylist'
import {connect} from 'react-redux'

const mapStateToProps=state=>({

})

const mapDispatchToProps=dispatch=>({

})

export default connect(mapDispatchToProps,mapStateToProps)(PurchasePlaylist)