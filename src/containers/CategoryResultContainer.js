import CategorySearchResult from '../components/SearchComponents/categorySearchResult'
import {connect} from 'react-redux'

const mapStateToProps=state=>({

})

const mapDispatchToProps=dispatch=>({

})

export default connect(mapDispatchToProps,mapStateToProps)(CategorySearchResult)